# Capstone

# Steps to run API
1. Extract to folder
2. Open terminal in folder
3. run `yarn install`
4. run `yarn start`

# Steps to run Web App
1. Extract to folder
2. Open terminal in folder
3. run `cd client`
4. run `yarn install`
5. run `yarn start`