{
    "name": "capstone",
    "version": "1.0.0",
    "main": "index.js",
    "repository": {
      
    },
    "license": "MIT",
    "dependencies": {
      "concurrently": "^3.1.0"
    },
    "scripts": {
      "install": "(cd server && yarn) && (cd .. ) && (cd client && yarn)",
      "start": "concurrently \"cd client && yarn start\" \"cd server && yarn start\""
    }
  }